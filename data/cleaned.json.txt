<section>
    <h3>方法論</h3>
    <p>AI 使用方式：...</p>
    <p>2024-2100 複合年均增長率: <span id="cagr-val">計算中...</span>%</p>
    <p>總成長率: <span id="growth-val">計算中...</span>%</p>
</section>

<script>
fetch('data/cleaned.json')
    .then(response => {
        if (!response.ok) throw new Error('找不到數據');
        return response.json();
    })
    .then(data => {
        document.getElementById('cagr-val').innerText = data.cagr_percent;
        document.getElementById('growth-val').innerText = data.total_growth;
    })
    .catch(error => {
        console.error('錯誤:', error);
        document.getElementById('cagr-val').innerText = '讀取失敗';
    });
</script>